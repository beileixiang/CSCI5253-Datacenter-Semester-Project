#
# The original Google demo depended on gaving a service "redis-master" AND
# a service "redis-slave". We're using a single redis server, so we create
# two services that point to the same pod.
#
#
apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: mongodb
spec:
  ports:
    - protocol: "TCP"
      port: 27017
      targetPort: 27017
  selector:
<<<<<<< HEAD
    app: mongodb
=======
    app: mongodb
>>>>>>> 2d7c39a7850425a1643245de4ca82d5662a5e70e
